package Carcassone.network.shared;

/**
 * Az osszes lehetseges uzenet tipusa a szerver es kliens kozott
 */
public enum MessageType {

    // Userekkel kapcsolatos
    LOGIN,
    LOGIN_OK,
    LOGIN_ERROR,

    // Lobby roommal kapcsolatos
    ROOM_LIST,
    CREATE_ROOM,
    JOIN_ROOM,
    JOIN_AS_OBSERVER,
    JOIN_OK,
    JOIN_ERROR,
    START_GAME,

    // Jatekkal kapcsolatos
    GAME_STATE,
    YOUR_TURN,
    PLACE_TILE,
    PLACE_MEEPLE,
    SKIP_MEEPLE,
    GAME_OVER,

    // Egyeb uzenetek
    ERROR,
    PLAYER_LEFT,
    ROOM_UPDATE
}